import { styled } from "..";

export const Container = styled('div', {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    // minHeight: '100vh',
    justifyContent: 'center',

    backgroundColor: '#f6f7ff',
    
})
